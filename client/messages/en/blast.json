{
  "stat": {
    "symbols": "symbols",
    "callers": "callers",
    "endpoints": "endpoints",
    "crons": "cron/jobs"
  },
  "statHelp": {
    "symbols": "Top-level functions/classes changed in this PR",
    "callers": "Places elsewhere in the codebase that call the changed symbols",
    "endpoints": "HTTP endpoints reachable from the impacted callers",
    "crons": "Scheduled jobs reachable from the impacted callers"
  },
  "view": {
    "tree": "Tree",
    "graph": "Graph"
  },
  "viewHelp": {
    "tree": "Expandable list: changed symbols → callers → endpoints",
    "graph": "Node-link diagram of the first changed symbol's impact"
  },
  "hint": "Click a symbol to expand its callers · click a file:line to open the code",
  "expandAll": "Expand all",
  "collapseAll": "Collapse all",
  "refresh": "Refresh",
  "openCode": "Open {loc} in the Files tab",
  "callerCount": "{count, plural, one {# caller} other {# callers}}",
  "noDownstream": "{count, plural, one {# changed symbol} other {# changed symbols}}, no downstream callers found.",
  "badgeHelp": {
    "endpoint": "HTTP endpoint reachable from this caller",
    "cron": "Scheduled job reachable from this caller"
  },
  "legend": {
    "changed": "changed symbol",
    "caller": "caller",
    "endpoint": "endpoint / cron"
  },
  "role": {
    "business": "business",
    "test": "test",
    "boilerplate": "boilerplate"
  },
  "roleHelp": {
    "business": "Reaches a real product surface (HTTP endpoint / cron) or a widely depended-on file",
    "test": "Caller is a test/spec/mock — lower production impact",
    "boilerplate": "Config / migration / generated code"
  },
  "risk": {
    "breaking": "breaking API",
    "loop": "in loop",
    "unguarded": "unguarded",
    "throws": "throws"
  },
  "riskHelp": {
    "breaking": "This PR changed the symbol's signature/interface — callers & integrations may break",
    "loop": "Called inside a loop — amplified performance impact",
    "unguarded": "This call site has no try/catch and the changed code can throw — stability risk",
    "throws": "The changed code adds a throw in this PR"
  },
  "finding": "{count, plural, one {# finding} other {# findings}}",
  "findingHelp": "{count, plural, one {# existing agent finding} other {# existing agent findings}} on this changed code (worst: {severity})",
  "notReviewed": "No agent has reviewed this PR yet — run one to cross-reference findings.",
  "dead": {
    "title": "Changed but not called externally",
    "tag": "no external callers",
    "help": "Exported/top-level symbols changed in this PR that nothing else calls — possibly dead code, or only used internally."
  },
  "related": {
    "title": "Prior PRs touching these files",
    "empty": "No other PRs have touched these files."
  },
  "overview": {
    "viewFull": "View full blast radius",
    "empty": "No blast radius yet."
  },
  "graph": {
    "empty": "No downstream callers to graph.",
    "ariaLabel": "Blast radius graph"
  }
}
