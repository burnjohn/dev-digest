# client/specs

Feature/UX specs for `@devdigest/web` — screen behaviour, states, and interaction
contracts, written *before* (or alongside) the code. One file per screen or feature
(e.g. `review-detail.md`, `agent-editor.md`).

Reference the relevant spec from a page/component or its PR when it exists.
