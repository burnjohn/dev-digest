# client — specs

Spec-Driven Development artifacts for `@devdigest/web`: a feature's **spec (the WHAT)** lives
here before its code (the HOW). Populated from L05 onward (`spec-creator → implementation-planner
→ implementer`). Keep specs authoritative and code-free.
