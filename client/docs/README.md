# client — docs

Curated design notes and reference for `@devdigest/web`. Per the root `CLAUDE.md` "Before
answering" rule, read what's here before diving into code. **Link to the source of truth,
don't duplicate it** — routes/columns are declared in code, i18n copy in `messages/`.
