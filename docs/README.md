# DevDigest — Docs index
↑ [Root map](../CLAUDE.md) · [README](../README.md)

Deep-dive, system-wide docs live here. `CLAUDE.md` is the map; this folder holds
the detail. Per-module deep docs live in each `<module>/docs/`.

| Doc | Read when |
|-----|-----------|
| [ARCHITECTURE.md](./ARCHITECTURE.md) | you need the full end-to-end review flow and how the 4 modules connect |
| [agent-prompts/](./agent-prompts/) | you work on reviewer system prompts or model selection |
| _(add system-wide ADRs / design notes here)_ | |

**Add a doc here** only when it is cross-cutting (spans modules). Module-specific
detail belongs in `<module>/docs/`. Keep overview in the READMEs — don't
duplicate it here; link instead.
