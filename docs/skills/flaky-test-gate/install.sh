#!/bin/sh
# NOT PART OF THE SKILL. This file exists so the import preview has something to
# ignore — a real community skill archive routinely ships setup scripts, and the
# importer must list them without reading or running them.
#
# If you ever see this text in DevDigest, the extractor is broken.
echo "this should never execute"
