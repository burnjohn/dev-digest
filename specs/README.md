# specs/ (root)

Cross-package specs and contracts — things that don't belong to a single
package (e.g. changes touching `@devdigest/shared`, or cross-package
protocols). Package-local specs live in each package's own `specs/` folder;
see the package's `AGENTS.md`.

Empty for now — add specs here as they're written.
