# reviewer-core — docs

Curated design notes and reference for `@devdigest/reviewer-core`. Per the root `CLAUDE.md`
"Before answering" rule, read what's here before diving into code. The engine is pure
(`diff → prompt → LLM → grounded findings`); document invariants here, not I/O.
