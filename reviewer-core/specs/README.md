# reviewer-core — specs

Spec-Driven Development artifacts for `@devdigest/reviewer-core`: a feature's **spec (the WHAT)**
lives here before its code (the HOW). Populated from L05 onward. Keep specs authoritative and
code-free.
