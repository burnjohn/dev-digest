# server — Specs index
↑ [CLAUDE.md](../CLAUDE.md)

Behavior contracts for the API. Empty for now.

The live contract is the set of route zod schemas (`@devdigest/shared`), which
drive both validation and serialization. Add API behavior specs here (e.g.
endpoint contracts, error-envelope expectations) rather than restating the
schemas.

| Spec | Covers |
|------|--------|
| _(add specs here)_ | |
