# server — docs

Curated design notes and reference for `@devdigest/api`. Per the root `CLAUDE.md` "Before
answering" rule, read what's here before diving into code. **Link to the source of truth,
don't duplicate it** — e.g. reviewer system prompts live in `docs/agent-prompts/` (repo root)
and the DB schema is the code in `src/db/schema/`.
