# server — Docs index
↑ [CLAUDE.md](../CLAUDE.md) · [README](../README.md)

Deep-dive backend docs live here. `CLAUDE.md` is the map; the README is the
overview (request/DI flow, API map, review context); this folder is the detail.
Empty for now — add docs as backend design decisions accrue.

| Doc | Read when |
|-----|-----------|
| _(add API design / DB schema / adapter design notes here)_ | |

Keep the request flow, API map, and env table in [`../README.md`](../README.md);
link to them, don't copy them.
