{
  "name": "Skills Lab lists the seeded skills",
  "description": "Loads /skills and confirms the seeded test-quality skills render as SkillCards, and that the sidebar's new SKILLS LAB section is present. Exercises the /skills route and GET /skills (including the used_by join, which is what renders the 'N agents' meta row). Read-only against seeded data — no model call.",
  "steps": [
    { "cmd": ["open", "{BASE}/skills"], "label": "load the Skills Lab" },
    { "cmd": ["wait", "--url", "/skills"], "label": "skills route reached" },
    { "cmd": ["wait", "--load", "networkidle"], "label": "skills fetch settles" },
    { "cmd": ["wait", "--text", "SKILLS LAB"], "label": "the new sidebar section renders" },
    { "cmd": ["wait", "--text", "uncovered-branch-gate"], "label": "seeded skill card is visible" },
    { "cmd": ["wait", "--text", "flaky-test-patterns"], "label": "a second seeded skill renders" },
    { "cmd": ["wait", "--text", "1 agent"], "label": "used_by count rendered from the join" },
    {
      "cmd": ["find", "text", "uncovered-branch-gate"],
      "label": "the card resolves as a deterministic locator",
      "assert": { "stdoutIncludes": "uncovered-branch-gate" }
    }
  ]
}
