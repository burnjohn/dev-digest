# e2e/docs

Design notes for the browser suite that are **too long for [AGENTS.md](../AGENTS.md)** —
the hermetic-stack design, agent-browser command reference, why we avoid the AI `chat`
command, and how CI wires `e2e-web.yml`.

The flow specs themselves live in [`../specs`](../specs); this folder is for the *why*.
