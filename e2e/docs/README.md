# e2e/docs

Deep-dive docs for `@devdigest/e2e` (flow design notes, agent-browser
setup) that are too long-lived or detailed for `e2e/README.md`.

Note: `e2e/specs/` already exists and holds the `*.flow.json` deterministic
flow specifications themselves — that folder is the "specs" this package's
`AGENTS.md` links to, not a new one.

Empty for now — add docs here as they're written.
