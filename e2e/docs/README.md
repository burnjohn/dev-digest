# e2e — docs

Curated notes for `@devdigest/e2e`. Per the root `CLAUDE.md` "Before answering" rule, read what's
here before diving into code. Flows are deterministic agent-browser specs in `specs/*.flow.json`;
document flow conventions and locator gotchas here.
